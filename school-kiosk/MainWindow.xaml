<Window x:Class="SchoolKiosk.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:webview2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        mc:Ignorable="d"
        Title="Школьное расписание" 
        WindowStyle="None" 
        ResizeMode="NoResize"
        WindowState="Maximized"
        Topmost="True"
        Background="#0a1929"
        AllowsTransparency="False"
        ShowInTaskbar="True"
        Name="KioskWindow">

    <Grid>
        <!-- WebView2 для отображения React-интерфейса -->
        <webview2:WebView2 x:Name="webView" 
                          HorizontalAlignment="Stretch" 
                          VerticalAlignment="Stretch" />

        <!-- Скрытый индикатор для администратора (по желанию) -->
        <Border x:Name="AdminIndicator" 
                HorizontalAlignment="Right" 
                VerticalAlignment="Top" 
                Background="#FF4444" 
                Width="20" 
                Height="20" 
                Margin="10,10,10,0" 
                CornerRadius="10"
                Visibility="Collapsed" 
                ToolTip="Режим администратора"/>
    </Grid>
</Window>